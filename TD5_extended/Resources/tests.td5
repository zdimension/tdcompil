{
	type square_impl = struct { };
	impl square_impl
    {
        func `(`(x: u16): u16
        {
            return x * x;
        }
    }

    type applier<T> = struct { };
    impl applier
    {
    	func `(`(x: u16): u16
    	{
    		return T(x);
    	}
    }

	print applier<square_impl>(5);
}