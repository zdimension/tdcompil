{
  var n = 10;

  // Tests de while
  while (n > 0)   { print(n); n = n-1; }
  while (n <= 10) { print(n); n = n+1; }

  // Tests de if
  if (1 < 2) print(1); else print(-1);
  if (2 < 1) print(-1); else print(1);
  // Le célèbre "dandling else"
  if (1) 
    if(0)
      print(-1);
    else
      print(1);
}
